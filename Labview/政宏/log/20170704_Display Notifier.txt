20170704
Support>Display Notifier
-----
    主要內容物
    一個Claster，裡面放queue(共六個)，queue的構成是variant。
    明明是Notfifier，裡面卻用queue的元件?
    All Display Notifiers In:DataArray、X、Y、Z、R、Data

Create All Display Notifiers
    Obatain 六個只包含variant的Queue，在bundle起來。

Send Notifier
    純粹Enqueue， Notifier In(queue)、Data(Element)

Switch Notifier
    利用enum(station)切換不同的Notifier Item，在此處是切換X、Y、Z、R


Close All Notifier
    ReleaseQueue



---------------

All Display Notifiers In可以自行塞入需要的元件
根據案子不同，全部都需要更改不同的元件，但是架構可以維持